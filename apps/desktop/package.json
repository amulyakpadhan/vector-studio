{
  "name": "@vyn/desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "web:dev": "pnpm --filter @vyn/web dev",
    "web:build": "pnpm --filter @vyn/web build",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
