/** Single source of truth for outbound links, so a repo/URL change is a
 * one-line edit instead of a hunt across components. */
export const GITHUB_URL = "https://github.com/amulyakpadhan/vector-studio";
