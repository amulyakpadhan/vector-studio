{
  "name": "vector-studio",
  "private": true,
  "version": "0.1.0",
  "description": "The universal studio for vector databases — connect, browse, search, and visualize any vector DB.",
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "bridge": "pnpm --filter @vyn/bridge start"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
