{
  "name": "@vyn/viz",
  "version": "0.1.0",
  "description": "Embedding projection engine for Vyn Studio — dimensionality reduction (UMAP) + a Three.js point-cloud renderer with query overlay.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./render": "./src/render/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "three": "^0.170.0",
    "umap-js": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0",
    "@types/three": "^0.170.0"
  }
}
